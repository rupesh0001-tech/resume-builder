create userRoutes
    userRouter.post('name) syntx 
  ther

add this route in server.js
    on /api/auth 

use thunderclint and test apis 

models > resume.models 
  create and connect a user with ref if 
  schema should have 
  title, rhumnailLink, template , profileinfod{},contact info {}
  expirence education, skills, projects certifications, languagues, interest, 
  at end add a timestap export as resume model 


make a resume controller 
  create resume
  try{
      extract title 
      paste default template
      add that data 
      give status 
  }catch{
    res(500)
  }

  getUserResume {
    find resume userid;

  }

  getresumebyId {
    try{
      find using id
      handel for resume not found 
      res.json ()
    }catch{

    }
  }

  updateResume 
      try {
        find using id 
        _id : params.id
        userId : req,user._id 
        Object assign 
      }catch{
        resturn error 
      }

deleteResume 
  try 
    find by id 
    handel not found 
    mk dir uploads 
    
